<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISOS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_perfil"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_perfil"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prueba1Integrador"
        android:usesCleartextTraffic="true">

        <!-- 🔑 GOOGLE MAPS API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- ACTIVIDAD PRINCIPAL -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".activity.GestionarInventarioActivity"
            android:exported="false"
            android:parentActivityName=".activity.HomeActivity"/>

        <activity
            android:name=".activity.AnadirProductoActivity"
            android:exported="false"
            android:parentActivityName=".activity.HomeActivity"/>

        <activity
            android:name=".model.CrearIncidencia"
            android:exported="false"/>

        <activity
            android:name=".activity.GestionAdminsActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".activity.DetalleCestaActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.PagoActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.AlquilarJuegoActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.HistorialActivity"
            android:exported="false" />

        <activity
            android:name=".fragment.FragmentMisPedidos"
            android:exported="false"/>

        <!-- MAPS ACTIVITY -->
        <activity
            android:name=".activity.MapsActivity"
            android:exported="false" />

    </application>

</manifest>